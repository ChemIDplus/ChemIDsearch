/* tslint:disable:no-any */
export const environment :any = {
	production: false,
	loggerLevel: 'LOG', /* You have to restart the ngServe if you change these. For loggerLevel, consider just temporarily hardcoding value in Logger.ts instead.  */
	apiUrl: 'https://chem.nlm.nih.gov/api/'
};
